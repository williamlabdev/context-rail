# ContextRail — demo video script (target ≤2:40, hard limit 3:00)

Format: screen recording of the workspace with voice-over, English narration throughout; the zh-TW / en locale switch (UI-16) is demonstrated **visually only** in beat 8, no Chinese narration. As of 2026-09-26 beats 1–5 and 5b replay a **real, already-recorded** governance state (`evidence/EB-012/`: Change `CHG-001` on the real repository `github.com/williamlabdev/order-operations-portal`, PR #1, CI check runs, the refused first candidate, the accepted second one — checked 2026-09-21). Beats 6–7 need a **release promoted through Cloud Run**, which cannot happen before Google Cloud access lands (**expected 2026-10-06**); those beats are recorded once staging and prod-demo exist and before the 2026-10-18 submission deadline. Until then this script is the shot list, not a transcript of a finished recording. The browser journeys in `tests/browser/*.spec.ts` remain the fallback for any beat that cannot be shown live.

`[UPDATE BEFORE SUBMISSION]` **Deployment status: not deployed yet.** No beat below should be read as "already shown on the live URL" until beats 6–7 are actually recorded post-deploy.

| # | Time | Screen | Voice-over (draft) |
| --- | --- | --- | --- |
| 1 | 0:00–0:11 | Title card → workspace header on `<DEPLOYED_URL — fill after 2026-10-06>`, Project **Order Operations Portal**, repository chip `github.com/williamlabdev/order-operations-portal · active` (only shows `active`, not `fixture-pending-remote`, when the server's fixture root is `tmp/order-operations-portal` — see recording checklist) | "AI coding agents ship changes fast. ContextRail proves a change still matches what was accepted at every governed environment it moves through — and blocks it when it doesn't. A real run, on a real GitHub repository." |
| 2 | 0:11–0:25 | Environment Topology: development / testing / staging / prod-demo / production (production read-only, `Topology v1`, config hash); hover a status badge to show the human label with its machine code kept small underneath | "A Project declares its context contract and its promotion path. Every topology change is an immutable, hashed version; production stays read-only. Every status reads in plain language first — the machine code stays secondary." |
| 3 | 0:25–0:47 | Changes → `CHG-001 Order exception evidence links` (REQ-002): the request table (objective, AC-001…005, allowed paths, forbidden actions), `DECISION_READY`, the three advisor options, then the recorded decision **DEC-001 · founder-001 · minimal_reversible_slice** with its rationale | "A request is evaluated deterministically. When an input is missing, the owner is named — nothing invented. An advisor proposes options: rule-based by default, Gemini when a key is configured — in this recording, the rule advisor ran. A named human decides; the decision binds to a source-snapshot hash and a topology version." |
| 4 | 0:47–1:02 | Change Decision Brief opened by default (no empty "Select a Change" state): plain-language summary first, scope shown as one merged line when nothing is excluded, "Considered, not chosen" set off from the decision rationale by a divider; Agent Context Pack and Work Order **AWO-001**: `work_order_hash sha256:952c8da4…` | "One decision renders two views: a Brief for people, plain language first, and a Context Pack for the agent — same id, same snapshot hash. The Work Order the agent receives is hashed: a contract, not a chat." |
| 5 | 1:02–1:38 | Candidate **CAND-001**: header `ARR-002 by williamlabdev · claude-cowork · observed via github`; gate table — `allowed_paths PASS`, `required_checks PASS`, non-passing rows only, `independent_review NEEDS_REVIEW` with the *missing* line; the accept button disabled. Cut to GitHub: PR #1, the two check runs, the AI review comment. Back: **CAND-002** with `independent_review WAIVED`, verdict `CANDIDATE_ACCEPTABLE`, human decision `ACCEPTED by founder-001` | "The agent — Claude, in this run — pushed a branch and opened a pull request. ContextRail doesn't trust the agent's report: it reads the branch, diff and check runs back from GitHub. Every technical gate passed — the candidate was still refused: no independent review. The gate blocks; it never approves. After a separate AI review and documented single-operator controls, policy allows a staging-only waiver. The second human decision accepts it." |
| 5b | 1:38–1:45 | Still from `evidence/EB-007/candidate-review.png` (fixture journey): a candidate `BLOCKED` with `infra/iam/public-bucket.yaml` named | "And when an agent steps outside the allowed paths, the candidate is blocked with the path named." |
| 6 | 1:45–2:08 | Release **REL-001**: bundle gate → build digest from `main` including `8853114` → third approval bound to the manifest hash → deployment record → **Release Receipt**. Open the release: one plain-language outcome sentence first ("Promoted to staging: revision … Receipt RR-001"), gate tables collapsed to "N of M gates passed"; toggle **Summary → Technical report** to reveal the full hash chain, manifest table and every deployment attempt | "A release bundles accepted candidates into one outcome sentence — pass or fail — before any table. The decision-maker's view stops there; a Technical report, one click away, carries the full hash chain, manifest and every attempt for whoever audits it." |
| 7 | 2:08–2:22 | Promote to prod-demo: environment delta table → new approval → staging revision refused (`new_revision` blocked) → new revision → chained receipt (`previous_receipt_id`) | "The same digest goes to prod-demo. The environment differences are listed and bound by a new approval; reusing the staging revision is refused; the second receipt chains to the first." |
| 8 | 2:22–2:33 | Edit staging target after approval → release `STALE`; switch the header language toggle to 繁中 for one screen — badges relabel (e.g. `已核准 ACCEPTED`), no voice-over change; Documents: declared runbook missing → `NEEDS_INPUT`, Context Pack `PARTIAL` | "When anything moves — the environment, the sources, a document — dependent decisions go stale instead of being silently reused. The locale switch relabels every status; the underlying data never changes." |
| 9 | 2:33–2:40 | Close: architecture strip (Cloud Run · Cloud Build · Artifact Registry · Gemini-when-configured), both public repos, `evidence/EB-012` folder | "Go on Cloud Run, built by Cloud Build, promoted by digest. Every slice ships with a DecisionRecord and an Evidence Bundle." |

Timecode table total: **2:40** (160 seconds) — this is the authoritative length, summed directly from the per-beat times above. Narration: 374 words, which at ~140 wpm is also ≈2:40 of speech — the pacing is tight against the table with no internal slack, so expect to trim a phrase or two once a real read-through is timed; nothing here assumes cuts land for free. Hard limit is 3:00, so there is roughly 20 seconds of margin beyond the table if a beat runs long in the actual recording.

## What this script does NOT claim

- No deployed URL exists yet — beat 1's screen target is a placeholder, not a live shot.
- No Gemini output has been observed by a human yet; beat 3 says "in this recording, the rule advisor ran" rather than showing or claiming a Gemini response. `[VERIFY AFTER DEPLOY]` if a real `GEMINI_API_KEY` run happens before 10/18 and is worth swapping in.
- No Firestore, Cloud Storage or embedding-based feature is mentioned — none exists in the code.
- ADR-002 (A2A extension) is **not** in this script. It is Proposed, not implemented, and belongs in the deck's roadmap slide only.
- The human reader test (roadmap item G5 / readability review item 6) has not been administered — the script does not claim the Brief has been proven readable by a naive reader, only that the AI-preview fixes (`fix/brief-reader-preview`) are live in the UI it records.
- Beat 8 shows zh-TW, it does not narrate in zh-TW — the whole video is English narration end to end.

## Recording checklist

- **Fixture root matters — do not record against `demo/order-operations-portal`.** That fixture's `project.yaml` declares `url: github.com/example/order-operations-portal` and `status: fixture-pending-remote`; the repository chip in beat 1 would show the placeholder repo, not the real one. Beats 1–5 and 5b need the **real governed clone**, `tmp/order-operations-portal` (its `project.yaml` says `url: github.com/williamlabdev/order-operations-portal`, `status: active`), served together with the recorded state that already contains `CHG-001` / `DEC-001` / `AWO-001` / `CAND-001` / `CAND-002`.
- **Both `tmp/order-operations-portal/` and `.context-rail-state/real-run/` are git-ignored** (`.gitignore` lines 6 and 12) — they are not in this repository's version control and will not exist in a fresh clone or a fresh worktree. As of this writing they exist only on the operator's main checkout, `/Users/william/dev/source/projects/context-rail/` (created 2026-09-21 per `evidence/EB-012/README.md` and `docs/operations/REAL_RUN_PLAYBOOK.md:10`). To record:
  - **Preferred — record from the machine where they already exist**: run the server directly from `/Users/william/dev/source/projects/context-rail/` (or copy both directories into whatever checkout does the recording, including a fresh worktree if that's where recording happens).
  - **If that machine is unavailable**: `.context-rail-state/real-run/` is ContextRail's own recorded ledger (the actual `DEC-001`/`CAND-001` entries) and **cannot be regenerated by cloning the source repo** — it has to be copied from wherever it currently lives, or the real run (`docs/operations/REAL_RUN_PLAYBOOK.md`) has to be re-executed end to end, which will mint new ids (e.g. `ARR-003` per that doc) and no longer match the ids named in this script. The clone itself, if it needs rebuilding, comes from `git clone https://github.com/williamlabdev/order-operations-portal.git tmp/order-operations-portal` (`docs/operations/REAL_RUN_PLAYBOOK.md:17`).
  - Start the server with the real-run state and the real clone as fixture root:
    ```sh
    PORT=8080 CONTEXT_RAIL_FIXTURE_ROOTS=tmp/order-operations-portal \
      CONTEXT_RAIL_STATIC_DIR=frontend/dist CONTEXT_RAIL_STATE_DIR=.context-rail-state/real-run \
      ./bin/context-rail
    # open http://127.0.0.1:8080/projects/order-operations-portal
    ```
- Beat 5 needs GitHub only for the *cut* to PR #1 (`https://github.com/williamlabdev/order-operations-portal/pull/1`); the ContextRail screens are the saved observation, no token needed while recording.
- Beat 5b: use the `evidence/EB-007/candidate-review.png` screenshot, or run `tests/browser/candidate-review.spec.ts` once on a scratch state dir for a live shot.
- **Beats 6–7 are gated on Cloud Run access (expected 2026-10-06).** Once staging and prod-demo exist: `scripts/bootstrap-w1.sh`, then `scripts/record-promotion.sh` for staging and prod-demo; record after both receipts exist, then replay the promotion in the UI for the camera. `[CAPTURE: REL-001 outcome sentence, gate-collapse, Summary/Technical toggle]`, `[CAPTURE: prod-demo delta + chained receipt]`. If Cloud Run access slips past a safe internal buffer before 2026-10-18, fall back to the local/fixture equivalents already proven in `evidence/EB-008/` and `evidence/EB-009/` and say so in the video's closing card rather than implying a live deployment.
- Beat 8's zh-TW badge relabel: confirm on the actual build which status the recorded Change is in and quote that label instead of a guessed one — `[CAPTURE: exact zh-TW badge text for the recorded state]`.
- Beat 9's architecture strip: `[VERIFY AFTER DEPLOY]` that Cloud Run / Cloud Build / Artifact Registry are actually the services in use once `scripts/deploy-cloud-run.sh` has run for real; today this is the documented plan, not an observed deployment.
- Locale: 繁中 for beat 8 only, shown on screen, no narration change; back to EN visuals for beat 9.
- Browser 1440×900, bookmarks bar hidden, gate tables expanded before the shot (`Show gates`) except where the collapsed "N of M gates passed" state is the point (beat 6).
- Say "Claude" once when the agent is named (beat 5); the run record says `claude-cowork`, not Claude Code — keep the narration accurate.

## Placeholders to resolve before submission

Grep this file for `[UPDATE BEFORE SUBMISSION]`, `[VERIFY AFTER DEPLOY]`, `[CAPTURE`, and `<DEPLOYED_URL` before the 2026-10-18 submission — every hit below must be resolved or explicitly re-confirmed:

- `[UPDATE BEFORE SUBMISSION]` — the top-of-file "not deployed" status line.
- `<DEPLOYED_URL — fill after 2026-10-06>` — beat 1's screen target.
- `[CAPTURE: REL-001 outcome sentence, gate-collapse, Summary/Technical toggle]`, `[CAPTURE: prod-demo delta + chained receipt]`, `[CAPTURE: exact zh-TW badge text for the recorded state]` — recording checklist.
- `[VERIFY AFTER DEPLOY]` — beat 3 / beat 9 Gemini narration, beat 9's architecture-strip checklist line.
