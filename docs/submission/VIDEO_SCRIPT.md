# ContextRail — demo video script (target 2:45, hard limit 3:00)

Format: screen recording of the workspace with voice-over, English narration, one beat in zh-TW to prove UI-16. Since 2026-09-21 the story is **real, not a fixture**: beats 3–5 replay the governance state of `evidence/EB-012/` (Change `CHG-001` on the real repository `github.com/williamlabdev/order-operations-portal`, PR #1, CI check runs, the refused first candidate, the accepted second one). Beats 6–8 are recorded on 10/8 once the same candidate is released to Cloud Run staging and prod-demo, so the whole video is one continuous change. The browser journeys in `tests/browser/*.spec.ts` remain the fallback for any beat that cannot be shown live.

| # | Time | Screen | Voice-over (draft) |
| --- | --- | --- | --- |
| 1 | 0:00–0:12 | Title card → workspace header on the Cloud Run URL, Project **Order Operations Portal**, repository chip `github.com/williamlabdev/order-operations-portal · active` | "AI coding agents ship changes fast. ContextRail is the control plane that proves, before each governed environment, that a change still matches what was accepted — and blocks it when it doesn't. Everything you'll see happened on a real GitHub repository." |
| 2 | 0:12–0:30 | Environment Topology: development / testing / staging / prod-demo / production (production read-only, `Topology v1`, config hash) | "A Project declares its context contract and its promotion path. Every topology change is an immutable version, hashed; production stays read-only in this prototype." |
| 3 | 0:30–0:55 | Changes → `CHG-001 Order exception evidence links` (REQ-002): the request table (objective, AC-001…005, allowed paths `main.go main_test.go web/index.html runs/`, forbidden actions), `DECISION_READY`, the three advisor options, then the recorded decision **DEC-001 · founder-001 · minimal_reversible_slice** with its rationale | "A request is evaluated deterministically. When an input is missing the owner is named; nothing is invented. The advisor proposes three options — here the rule advisor, Gemini when a key is present — and a named human decides. This decision is bound to a source-snapshot hash and a topology version." |
| 4 | 0:55–1:10 | Brief and Agent Context Pack with the same lineage strip; Work Order **AWO-001** card: `work_order_hash sha256:952c8da4…`, `change/chg-001 ← main`, expiry | "One DecisionRecord renders two views: a Brief for people and a Context Pack for the agent — same decision id, same snapshot hash. The Work Order the agent receives is hashed; the agent gets a contract, not a chat." |
| 5 | 1:10–1:50 | Candidate **CAND-001**: header `ARR-002 by williamlabdev · claude-cowork · observed via github`, `change/chg-001 @ 8853114 · 4 path(s)`; gate table — `allowed_paths PASS`, `required_checks PASS` (hover the check-run URL), `independent_review NEEDS_REVIEW` with the *missing* line; the accept button disabled. Cut to GitHub: PR #1, the two check runs, the AI review comment. Back: **CAND-002** with `independent_review WAIVED`, verdict `CANDIDATE_ACCEPTABLE`, human decision `ACCEPTED by founder-001` | "The agent — Claude, in this run — pushed a branch and opened a pull request. ContextRail does not trust the agent's report: it reads the branch, the diff and the check runs back from GitHub. Every technical gate passed. The candidate was still refused: no independent review. The gate blocks; it never approves. After a separate AI review and documented single-operator controls, the policy allows a waiver for staging only — and the second human decision accepts the candidate." |
| 5b | 1:50–1:58 | Still from `evidence/EB-007/` (fixture journey): a candidate `BLOCKED` with `infra/iam/public-bucket.yaml` named | "And when an agent steps outside the allowed paths, the candidate is blocked with the path named." |
| 6 | 1:58–2:20 | Release **REL-001** (10/8): bundle gate → build digest from `main` including `8853114` → approval bound to the manifest hash → deployment record from `record-promotion.sh` (digest / target / config / smoke) → **Release Receipt RR-001** | "A release bundles accepted candidates. The build must include the accepted commit; the approval is bound to the manifest hash; the deployment is verified against what Cloud Run actually runs — digest, target, configuration, smoke — and the receipt links back to the decision." |
| 7 | 2:20–2:38 | Promote to prod-demo: environment delta table → new approval → staging revision refused (`new_revision`) → new revision → chained receipt (`previous_receipt_id`) | "The same digest goes to prod-demo. The environment differences are listed and bound by a new approval; re-using the staging revision is refused; the second receipt chains to the first." |
| 8 | 2:38–2:50 | Edit staging target after approval → release `STALE`; switch to 繁中 for one screen; Documents: declared runbook missing → `NEEDS_INPUT`, Context Pack `PARTIAL` | "When anything moves — the environment, the sources, a document — dependent decisions go stale instead of being reused silently. 而且介面本身也說中文。" |
| 9 | 2:50–3:00 | Close: architecture strip (Cloud Run · Cloud Build · Artifact Registry · Gemini), both public repos, `evidence/EB-012` folder | "Go on Cloud Run, built by Cloud Build, promoted by digest, advised by Gemini. ContextRail is built the way it asks its users to build: every slice ships with a DecisionRecord and an Evidence Bundle — including this run." |

## Recording checklist

- **Seed the recording from the real run**: copy `context-rail/.context-rail-state/real-run/` to the `CONTEXT_RAIL_STATE_DIR` of the recording server (local or the Cloud Run image — bake it in as a seed directory copied on start) with the governed repository clone as the fixture root. Ids then read `CHG-001 / DEC-001 / AWO-001 / CAND-001 / CAND-002` exactly as in EB-012, and the 10/8 release continues the same state (`REL-001 / RR-001 / RR-002`).
- Beat 5 needs GitHub only for the *cut* to PR #1 (`https://github.com/williamlabdev/order-operations-portal/pull/1`); the ContextRail screens are the saved observation, no token needed while recording.
- Beat 5b: use `evidence/EB-007/` screenshot or run `tests/browser/candidate-review.spec.ts` once on a scratch state dir for a live shot.
- Beats 6–7 on 10/8: `scripts/bootstrap-w1.sh`, then `scripts/record-promotion.sh` for staging and prod-demo; record after both receipts exist, then replay the promotion in the UI for the camera.
- Locale: 繁中 for beat 8 only, one sentence, then back to EN.
- Browser 1440×900, bookmarks bar hidden, gate tables expanded before the shot (`Show gates`).
- Say "Claude" once when the agent is named (beat 5); the run record says `claude-cowork`, not Claude Code — keep the narration accurate.
