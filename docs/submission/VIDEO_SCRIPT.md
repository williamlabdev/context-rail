# ContextRail — demo video script (target 2:40–2:50, hard limit 3:00)

Format: screen recording of the workspace with voice-over, English narration, one beat in zh-TW to prove UI-16. As of 2026-09-26 beats 1–5 and 5b replay a **real, already-recorded** governance state (`evidence/EB-012/`: Change `CHG-001` on the real repository `github.com/williamlabdev/order-operations-portal`, PR #1, CI check runs, the refused first candidate, the accepted second one — checked 2026-09-21). Beats 6–8 need a **release promoted through Cloud Run**, which cannot happen before Google Cloud access lands (**expected 2026-10-06**); those beats are recorded once staging and prod-demo exist and before the 2026-10-18 submission deadline. Until then this script is the shot list, not a transcript of a finished recording. The browser journeys in `tests/browser/*.spec.ts` remain the fallback for any beat that cannot be shown live.

**Deployment status: not deployed yet.** No beat below should be read as "already shown on the live URL" until beats 6–8 are actually recorded post-deploy.

| # | Time | Screen | Voice-over (draft) |
| --- | --- | --- | --- |
| 1 | 0:00–0:11 | Title card → workspace header on `<DEPLOYED_URL — fill after 2026-10-06>`, Project **Order Operations Portal**, repository chip `github.com/williamlabdev/order-operations-portal · active` | "AI coding agents ship changes fast. ContextRail proves a change still matches what was accepted before each governed environment — and blocks it when it doesn't. What follows is a real run, on a real GitHub repository." |
| 2 | 0:11–0:26 | Environment Topology: development / testing / staging / prod-demo / production (production read-only, `Topology v1`, config hash); hover a status badge to show the human label with its machine code kept small underneath | "A Project declares its context contract and its promotion path. Every topology change is an immutable, hashed version; production stays read-only. Every status reads in plain language first — the machine code just steps back, never disappears." |
| 3 | 0:26–0:50 | Changes → `CHG-001 Order exception evidence links` (REQ-002): the request table (objective, AC-001…005, allowed paths, forbidden actions), `DECISION_READY`, the three advisor options, then the recorded decision **DEC-001 · founder-001 · minimal_reversible_slice** with its rationale | "A request is evaluated deterministically. When an input is missing, the owner is named — nothing invented. An advisor proposes options: rule-based by default, Gemini when a key is configured — here, the rule advisor. A named human decides; the decision binds to a source-snapshot hash and a topology version." |
| 4 | 0:50–1:06 | Change Decision Brief opened by default (no empty "Select a Change" state): plain-language summary first, scope shown as one merged line when nothing is excluded, "Considered, not chosen" set off from the decision rationale by a divider; Agent Context Pack and Work Order **AWO-001**: `work_order_hash sha256:952c8da4…` | "One decision renders two views: a Brief for people, plain language first, and a Context Pack for the agent — same id, same snapshot hash. The Work Order the agent receives is hashed: a contract, not a chat." |
| 5 | 1:06–1:45 | Candidate **CAND-001**: header `ARR-002 by williamlabdev · claude-cowork · observed via github`; gate table — `allowed_paths PASS`, `required_checks PASS`, non-passing rows only, `independent_review NEEDS_REVIEW` with the *missing* line; the accept button disabled. Cut to GitHub: PR #1, the two check runs, the AI review comment. Back: **CAND-002** with `independent_review WAIVED`, verdict `CANDIDATE_ACCEPTABLE`, human decision `ACCEPTED by founder-001` | "The agent — Claude, in this run — pushed a branch and opened a pull request. ContextRail doesn't trust the agent's report: it reads the branch, diff and check runs back from GitHub. Every technical gate passed — the candidate was still refused: no independent review. The gate blocks; it never approves. After a separate AI review and documented single-operator controls, policy allows a staging-only waiver, and the second human decision accepts it." |
| 5b | 1:45–1:53 | Still from `evidence/EB-007/` (fixture journey): a candidate `BLOCKED` with `infra/iam/public-bucket.yaml` named | "And when an agent steps outside the allowed paths, the candidate is blocked with the path named." |
| 6 | 1:53–2:18 | Release **REL-001**: bundle gate → build digest from `main` including `8853114` → third approval bound to the manifest hash → deployment record → **Release Receipt**. Open the release: one plain-language outcome sentence first ("Promoted to staging: revision … Receipt RR-001"), gate tables collapsed to "N of M gates passed"; toggle **Summary → Technical report** to reveal the full hash chain, manifest table and every deployment attempt | "A release bundles accepted candidates into one outcome sentence — pass or fail — before any table. The decision-maker's view stops there; a Technical report, one click away, carries the full hash chain, manifest and every attempt for whoever audits it." |
| 7 | 2:18–2:34 | Promote to prod-demo: environment delta table → new approval → staging revision refused (`new_revision` blocked) → new revision → chained receipt (`previous_receipt_id`) | "The same digest goes to prod-demo. The environment differences are listed and bound by a new approval; reusing the staging revision is refused; the second receipt chains to the first." |
| 8 | 2:34–2:47 | Edit staging target after approval → release `STALE`; switch header to 繁中 for one screen (badges relabel, e.g. `已核准 ACCEPTED`); Documents: declared runbook missing → `NEEDS_INPUT`, Context Pack `PARTIAL` | "When anything moves — the environment, the sources, a document — dependent decisions go stale instead of being silently reused. 而且介面本身也說中文，狀態一律先講人話。" |
| 9 | 2:47–2:57 | Close: architecture strip (Cloud Run · Cloud Build · Artifact Registry · Gemini-when-configured), both public repos, `evidence/EB-012` folder | "Go on Cloud Run, built by Cloud Build, promoted by digest — Gemini available as the advisor when a key is configured. ContextRail is built the way it asks its users to build: every slice ships with a DecisionRecord and an Evidence Bundle, including this run." |

Estimated narration: ~390 words at ~140 wpm ≈ 2:47, inside the 3:00 hard limit with roughly 13 seconds of buffer.

## What this script does NOT claim

- No deployed URL exists yet — beat 1's screen target is a placeholder, not a live shot.
- No Gemini output has been observed by a human yet; beat 3 and beat 9 both say "when configured" / "rule advisor" rather than showing or claiming a Gemini response. `[VERIFY AFTER DEPLOY]` if a real `GEMINI_API_KEY` run happens before 10/18 and is worth swapping in.
- No Firestore, Cloud Storage or embedding-based feature is mentioned — none exists in the code.
- ADR-002 (A2A extension) is **not** in this script. It is Proposed, not implemented, and belongs in the deck's roadmap slide only.
- The human reader test (roadmap item G5 / readability review item 6) has not been administered — the script does not claim the Brief has been proven readable by a naive reader, only that the AI-preview fixes (`fix/brief-reader-preview`) are live in the UI it records.

## Recording checklist

- **Seed the recording from the real EB-012 run** for beats 1–5: copy `context-rail/.context-rail-state/real-run/` to the `CONTEXT_RAIL_STATE_DIR` of the recording server (local, or the Cloud Run image once deployed — bake it in as a seed directory copied on start) with the governed repository clone as the fixture root. Ids then read `CHG-001 / DEC-001 / AWO-001 / CAND-001 / CAND-002` exactly as in EB-012.
- Beat 5 needs GitHub only for the *cut* to PR #1 (`https://github.com/williamlabdev/order-operations-portal/pull/1`); the ContextRail screens are the saved observation, no token needed while recording.
- Beat 5b: use the `evidence/EB-007/` screenshot, or run `tests/browser/candidate-review.spec.ts` once on a scratch state dir for a live shot.
- **Beats 6–8 are gated on Cloud Run access (expected 2026-10-06).** Once staging and prod-demo exist: `scripts/bootstrap-w1.sh`, then `scripts/record-promotion.sh` for staging and prod-demo; record after both receipts exist, then replay the promotion in the UI for the camera. `[CAPTURE: REL-001 outcome sentence, gate-collapse, Summary/Technical toggle]`, `[CAPTURE: prod-demo delta + chained receipt]`. If Cloud Run access slips past a safe internal buffer before 2026-10-18, fall back to the local/fixture equivalents already proven in `evidence/EB-008/` and `evidence/EB-009/` and say so in the video's closing card rather than implying a live deployment.
- Beat 8's zh-TW badge relabel: confirm on the actual build which status the recorded Change is in and quote that label instead of a guessed one — `[CAPTURE: exact zh-TW badge text for the recorded state]`.
- Beat 9's architecture strip: `[VERIFY AFTER DEPLOY]` that Cloud Run / Cloud Build / Artifact Registry are actually the services in use once `scripts/deploy-cloud-run.sh` has run for real; today this is the documented plan, not an observed deployment.
- Locale: 繁中 for beat 8 only, one sentence, then back to EN.
- Browser 1440×900, bookmarks bar hidden, gate tables expanded before the shot (`Show gates`) except where the collapsed "N of M gates passed" state is the point (beat 6).
- Say "Claude" once when the agent is named (beat 5); the run record says `claude-cowork`, not Claude Code — keep the narration accurate.
