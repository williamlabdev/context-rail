# EB-000: Engineering Evidence Bundle

Link this bundle to exactly one Request/Decision or explicitly list every Change in a release bundle. Record source, observed time, actor, commit/image identity, permission scope and evidence hash.

| Evidence | Status | Reference |
| --- | --- | --- |
| Diff / commit | `NEEDS_INPUT` | `<REPLACE_ME>` |
| Independent code review | `NEEDS_INPUT` | `<REPLACE_ME>` |
| Test result | `NEEDS_INPUT` | `<REPLACE_ME>` |
| Build identity | `NEEDS_INPUT` | `<REPLACE_ME>` |
| Environment configuration | `NEEDS_INPUT` | `<REPLACE_ME>` |
| Smoke / runtime result | `NEEDS_INPUT` | `<REPLACE_ME>` |

Do not mark the bundle `PASS` merely because a link exists. Evidence must match the approved scope and target identity.
