# Decision Records

Copy `decisions/DR-000-template.json` for each accepted, rejected or held Request. A DecisionRecord is immutable by version and must reference the source snapshot, policy version, assumptions, unknowns, selected option and human decision.
