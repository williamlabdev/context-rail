# Development Contract

## Prerequisites

- Runtime and toolchain: `<REPLACE_ME>`
- Dependency installation: `<REPLACE_ME>`
- Required local services or fixtures: `<REPLACE_ME>`

## Commands

```sh
<REPLACE_ME install command>
<REPLACE_ME test command>
<REPLACE_ME lint command>
<REPLACE_ME build command>
<REPLACE_ME run command>
```

## Verification evidence

State which command proves tests, lint, build, local smoke and any required fixture validation. Commands must be reproducible by a second user.

## Known limits and common failures

- `<REPLACE_ME>`
