# Project Architecture

## Scope and boundary

Describe the system boundary, repository paths, services, data owners and external dependencies. Explicitly state what this Project does not own.

## Component and data flow

```text
User / client
    ↓
<REPLACE_ME application components and data flow>
```

## Security and failure behavior

- Identity and authorization: `<REPLACE_ME>`
- Sensitive data classification: `<REPLACE_ME>`
- Failure and retry behavior: `<REPLACE_ME>`
- Observability and rollback: `<REPLACE_ME>`

## Change impact checklist

For each Request, assess affected services, API/data contracts, IAM, migrations, reliability, tests, cost and rollback conditions. Missing information stays `UNKNOWN` or `NEEDS_INPUT`.
